{
  "name": "best-shop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --files 'css/*.css, *.html' --browser 'google chrome' --open"
  },
  "browserslist": [
    "defaults"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "gulp": "^5.0.0",
    "gulp-autoprefixer": "^7.0.1",
    "gulp-sass": "^5.1.0",
    "node-sass": "^9.0.0",
    "sass": "^1.76.0"
  },
  "dependencies": {
    "gulp-sourcemaps": "^2.6.5"
  }
}
